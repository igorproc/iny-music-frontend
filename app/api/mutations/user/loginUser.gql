mutation loginUser($loginData: UserLogin!) {
    loginUser(loginData: $loginData) {
        uid
        username
        gender
        birthday
        phone
        avatarUrl
        email
        role
    }
}
